function updateResolution(stage){
    stage.canvas.width = stage.canvas.clientWidth;
    stage.canvas.height = stage.canvas.clientHeight;
    // console.log({stage, w: stage.canvas.width, h: stage.canvas.height});
}